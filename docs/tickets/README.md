# Tickets

Local Markdown issue tracker. Conventions and labels: `docs/agents/issue-tracker.md`.

- `wayfinder/issues/`: decision tickets charted by `/wayfinder`; the map is
  `docs/plans/wayfinder.md`.
- `phase-<N>/issues/`: implementation tickets from `/to-tickets` for each phase.
