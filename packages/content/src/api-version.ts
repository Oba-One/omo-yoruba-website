/**
 * The API version every Sanity read and write pins (docs/research/phase-2-sanity-client-and-image-url.md):
 * the Studio, the site's loaders, the actions and the functions. A plain module, so the site
 * reads it without importing the Studio configuration (whose plugins carry the Studio's
 * stylesheets into any page that reaches them).
 */
export const STUDIO_API_VERSION = '2026-09-11';
