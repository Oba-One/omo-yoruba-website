# @oy/content

Owns the content model: every Sanity type (`defineType`), Studio structure,
GROQ queries (`defineQuery`), the generated `sanity.types.ts`, the seed script
and the Sanity Functions. The only package where GROQ lives. Built in Phase 2
from `docs/design/CONTENT-MODEL.md`.
