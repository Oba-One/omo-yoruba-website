# @oy/tokens

Design tokens, base styles, component CSS, fonts and pattern SVGs. Ported in
Phase 1 from `docs/design/design/_ds/` and `docs/design/design/oy-components.css`.
Every colour on the site comes from here: `packages/ui` and `apps/web` use
`var(--*)` only (checked by `bun lint:colors`).
