// @oy/ui entry. Components are imported by path (`@oy/ui/core/Button/Button.astro`); this file
// exists so `tsc` has an input until Phase 1 switches the package's typecheck to `astro check`.
export {};
